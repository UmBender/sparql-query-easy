# Captured Outputs Only

Do not add manually invented expected outputs here. Each committed baseline must
be captured from the running C# service and include its case ID, source commit,
command/environment metadata, and allowed normalizations in `capture.md`.

See `../README.md` for the capture layout and comparison policy.
