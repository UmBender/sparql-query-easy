# Property paths are not generated by application methods

No `SparqlQueryBuilder` method produces a property path. Raw request fields are
interpolated into generated SPARQL, so a caller may place a path in `predicate`.
`SPARQL-INJECTION-PATH-001` records text generation only; it is not a safe
executable query case.
